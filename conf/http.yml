# BCSPanel http

# HTTP或HTTPS的监听端口。
# 类型: uint16 | string
# 默认: 24124
# 仅监听IPv4本地回环地址: "127.0.0.1:24124"
# 仅监听IPv6本地回环地址: "[::1]:24124"
listen: 24124
# 路径前缀
# 规范: 开头与结尾必须是正斜杠，中间不得包含[0-9a-zA-Z]以外的字符
# 类型: string
# 默认: /
# 示范: /bcspanel/
path_prefix: /
